name: Deployment
on: push
jobs:
  Deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Get repository code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Test app
        run: npm run test
      - name: Deploy app
        run: npm run deploy
